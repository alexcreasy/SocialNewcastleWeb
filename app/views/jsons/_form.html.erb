<%= form_for(@json) do |f| %>
  <% if @json.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@json.errors.count, "error") %> prohibited this json from being saved:</h2>

      <ul>
      <% @json.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br>
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :data %><br>
    <%= f.text_field :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
